{% render 'cc-structured-data' %}
{% liquid
  assign cc_newsletter = blank

  if cc_page and cc_page.newsletter != blank
    assign cc_newsletter = cc_page.newsletter
  endif
%}

<section class="section" style="background:var(--color-off-white);border-top:1px solid var(--color-warm-gray);">
  <div class="container" style="display:grid;grid-template-columns:minmax(0,1fr) minmax(0,.9fr);gap:var(--space-xl);align-items:start;">
    <div>
      {% if cc_newsletter.eyebrow != blank or section.settings.eyebrow != blank %}<div class="text-eyebrow">{{ cc_newsletter.eyebrow | default: section.settings.eyebrow }}</div>{% endif %}
      {% if cc_newsletter.heading != blank or section.settings.heading != blank %}<h2 class="text-h2" style="margin-top:1.25rem;margin-bottom:1rem;">{{ cc_newsletter.heading | default: section.settings.heading }}</h2>{% endif %}
      {% if cc_newsletter.copy != blank or section.settings.copy != blank %}<div class="text-body rte">{{ cc_newsletter.copy | default: section.settings.copy }}</div>{% endif %}
    </div>
    <div>
      {% form 'customer', class: 'signup-form' %}
        <input type="hidden" name="contact[tags]" value="newsletter">
        <div class="signup-form-row">
          <input type="email" class="signup-input" placeholder="Email address" required id="signup-email" name="contact[email]">
          <button type="submit" class="signup-btn">{{ cc_newsletter.button_label | default: section.settings.button_label }}</button>
        </div>
        <p class="signup-disclaimer">{{ cc_newsletter.disclaimer | default: section.settings.disclaimer }}</p>
        {% if form.posted_successfully? %}
          <div id="signup-success" style="display:block;" class="text-eyebrow">{{ cc_newsletter.success_text | default: section.settings.success_text }}</div>
        {% endif %}
        {{ form.errors | default_errors }}
      {% endform %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Newsletter",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Community Signup" },
    { "type": "html", "id": "heading", "label": "Heading", "default": "Join the AREN list." },
    { "type": "richtext", "id": "copy", "label": "Copy", "default": "<p>Drops, restocks, and community updates — sent when it matters.</p>" },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "Join" },
    { "type": "text", "id": "disclaimer", "label": "Disclaimer", "default": "No spam. Unsubscribe any time." },
    { "type": "text", "id": "success_text", "label": "Success text", "default": "You're on the list." }
  ],
  "presets": [{ "name": "Newsletter" }]
}
{% endschema %}
